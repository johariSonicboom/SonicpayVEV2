<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SonicPayUI" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#050F39</item>
        <item name="colorPrimaryVariant">#1137D0</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorButtonNormal">#1137D0</item>
        <item name="colorAccent">#018786</item>
        <item name="android:titleTextStyle">@style/Theme.SonicPayUI.ActionBar.TitleTextStyle</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">#5C5C5C</item>
        <item name="colorSecondaryVariant">#5C5C5C</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>

        <!-- Customize your theme here. -->
        <item name="android:textColor">#1E1E1E</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat</item>
    </style>

    <style name="Theme.SonicPayUI.ActionBar" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:titleTextStyle">@style/Theme.SonicPayUI.ActionBar.TitleTextStyle</item>
    </style>

    <style name="Theme.SonicPayUI.ActionBar.TitleTextStyle" parent="@android:style/Widget.TextView">
        <item name="android:textSize">20sp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="Theme.SonicPayUI.Button">
        <item name="android:titleTextStyle">@style/Theme.SonicPayUI.Button.TextStyle</item>
    </style>

    <style name="Theme.SonicPayUI.Button.TextStyle" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>